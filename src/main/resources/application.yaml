spring:
  profiles:
    active: mongo

  datasource:
    url: jdbc:postgresql://localhost:5432/dosw_library
    username: admin
    password: Cazafantasmas32
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  data:
    mongodb:
      uri: mongodb+srv://JuanDa:Cazafantasmas32@cluster0.s579smk.mongodb.net/?appName=Cluster0
      database: dosw_library

server:
  port: 8443
  ssl:
    enabled: true
    key-store: classpath:keystore.p12
    key-store-password: dosw2026
    key-store-type: PKCS12
    key-alias: dosw-library
