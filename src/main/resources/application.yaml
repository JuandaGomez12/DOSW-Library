spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dosw_library
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  data:
    mongodb:
      uri: mongodb+srv://JuanDa:TU_PASSWORD@cluster0.6hiv5lv.mongodb.net/dosw_library?retryWrites=true&w=majority&appName=Cluster0

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration
      - org.springframework.boot.autoconfigure.data.mongo.MongoDataAutoConfiguration

server:
  port: 8443
  ssl:
    enabled: true
    key-store: classpath:keystore.p12
    key-store-password: dosw2026
    key-store-type: PKCS12
    key-alias: dosw-library